package tools

const ToolSelectionRules = "Tool selection: filesystem tools (fs_*) can only access files within the workspace root shown above. For paths outside the workspace root (e.g. system directories like Downloads, Desktop, /tmp), use shell tools directly. On Windows shell_run executes via cmd /C and supports cmd.exe commands (dir, type, echo). On Windows, prefer powershell_run for PowerShell pipelines, variables, script blocks, filtering, counting, or querying; pass only the PowerShell command and do not prefix it with powershell, powershell.exe, pwsh, or -Command. Minimize tool calls: prefer a single well-formed command over multiple retries with minor variations. Shell output redirection (>, >>) writes files and triggers review. For filtering, counting, or querying, pipe commands together or use powershell_run with a script block (e.g. Get-ChildItem | Where-Object | Measure-Object). Avoid shell redirect (> file && type file), Out-File, Set-Content, or temporary .ps1 files just to see results when a pipeline or PowerShell script block suffices. For complex multi-step tasks that do require intermediate files, write inside the workspace root so fs_read_file can access them directly without triggering shell_run review."

const PosixShellRunDescription = "Run a shell command via sh and return stdout+stderr. Output is returned directly; do not redirect to a file just to see results. Pipe commands together for filtering, counting, or text processing (e.g. find ... | wc -l)."

const WindowsShellRunDescription = "Run a cmd.exe command via cmd /C and return stdout+stderr. Use this for cmd.exe builtins such as dir, type, and echo. For PowerShell pipelines, variables, filtering, counting, or querying, use powershell_run instead of nesting powershell -Command here. Output is returned directly; do not redirect to a file just to see results."

const PowerShellRunDescription = "Run a PowerShell command directly via powershell.exe -NoProfile -ExecutionPolicy Bypass -Command and return stdout+stderr. Pass only the PowerShell pipeline or script block; do not prefix with powershell, powershell.exe, pwsh, or -Command. Use this on Windows for filtering, counting, querying, and PowerShell variables such as $PSVersionTable or $_ without cmd /C quoting. Output is returned directly; do not use Out-File, Set-Content, shell redirection, or temporary .ps1 files just to see results."
